package main

func serv() {
	// start listen rabbitmq or http requests
	// in data id of ex, ex code with new filename, user id, lang, in data for ex
	// out data id user, id ex, compiler out if error, run output, also type error or ok
	// if some tests ok => status OK, else err
	// for each request we run function ExecuteProgram
}
